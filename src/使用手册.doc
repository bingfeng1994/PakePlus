服务器带宽流量采集与异常峰值告警系统
使用手册

========================================

目录

1. 流量实时监控
2. 历史数据分析
3. 异常峰值告警
4. 节点设备管理
5. 告警日志审计
6. 系统与策略设置

========================================

1. 流量实时监控

功能概述
流量实时监控模块是系统的数据感知中枢，实时采集并可视化展示各服务器节点的上下行带宽等核心网络指标。

主要功能

1.1 动态拓扑图
- 展示各服务器节点的网络连接关系
- 使用不同颜色标识节点状态：
  * 绿色：节点在线且正常
  * 红色：节点离线或异常
- 支持鼠标悬停查看详细数据
- 支持点击节点查看历史流量记录

1.2 实时折线图
- 展示各节点的上下行带宽数据
- 动态更新，反映最新网络状态
- 支持时间轴选择不同时间段
- 支持缩放和拖拽查看

1.3 统计卡片
- 总节点数：显示系统中所有监控节点数量
- 在线节点：显示当前在线的节点数量
- 离线节点：显示当前离线的节点数量
- 告警数量：显示当前待处理的告警数量

1.4 数据刷新
- 提供手动刷新按钮，获取最新数据
- 系统自动定时刷新数据

操作步骤

查看实时流量：
1. 点击左侧菜单"流量实时监控"
2. 查看网络拓扑图，了解节点连接状态
3. 查看实时流量趋势图，分析带宽使用情况
4. 鼠标悬停在图表上查看详细数据

刷新数据：
1. 点击"刷新数据"按钮
2. 系统立即获取最新的流量数据

========================================

2. 历史数据分析

功能概述
基于实时监控沉淀的海量流量数据，提供多维度的历史趋势回溯与深度统计。支持按日、周、月生成带宽利用率报表，并通过95计费峰值算法辅助成本核算。

主要功能

2.1 时间范围选择
- 按日：查看每日的流量数据
- 按周：查看每周的流量数据
- 按月：查看每月的流量数据
- 默认选中当前日期

2.2 统计摘要面板
- 总流量：统计周期内的总流量（GB）
- 最高峰值：统计周期内的最高带宽峰值（Mbps）
- 95计费峰值：基于95计费算法计算的峰值（Mbps）
- 平均带宽利用率：统计周期内的平均利用率（%）

2.3 趋势图表
- 带宽利用率趋势图：折线图展示带宽利用率变化
- 峰值流量统计图：柱状图展示峰值流量分布
- 支持图表缩放和悬浮提示
- 支持导出为图片或数据

2.4 报表导出
- 一键生成带宽利用率报表
- 支持导出为PDF或Excel格式
- 包含95计费峰值的相关数据

操作步骤

查看历史数据：
1. 点击左侧菜单"历史数据分析"
2. 选择时间范围（日/周/月）
3. 查看统计摘要面板，了解关键指标
4. 查看趋势图表，分析流量变化

导出报表：
1. 选择需要的时间范围
2. 点击"导出报表"按钮
3. 选择导出格式（PDF/Excel）
4. 系统自动生成并下载报表

========================================

3. 异常峰值告警

功能概述
实时接收流量监控数据并与预设阈值进行比对。支持静态阈值与基于历史基线的动态AI阈值，一旦检测到带宽突增等异常峰值，立即触发多级告警。

主要功能

3.1 告警统计卡片
- 待处理告警：显示当前待处理的告警数量
- 处理中告警：显示正在处理的告警数量
- 已确认告警：显示已确认的告警数量
- 平均响应时间：显示告警平均响应时间

3.2 告警列表
- 告警ID：告警的唯一标识
- 节点名称：触发告警的节点
- 告警级别：严重/警告/信息
- 告警类型：带宽突增/节点离线/流量峰值等
- 告警信息：详细的告警描述
- 触发时间：告警触发的时间
- 状态：待处理/处理中/已确认/已归档/误报
- 处理人：负责处理该告警的人员

3.3 告警筛选
- 按状态筛选：待处理/处理中/已确认/已归档/误报
- 按级别筛选：严重/警告/信息
- 支持关键词搜索

3.4 告警处理
- 查看详情：查看告警的完整信息
- 处理告警：指派处理人并添加处理备注
- 标记误报：将误报的告警标记为误报
- 归档告警：将已处理的告警归档

告警级别说明

严重（Critical）：
- 带宽超过阈值90%以上
- 节点完全离线
- 需要立即处理

警告（Warning）：
- 带宽超过阈值80%-90%
- 节点探针异常
- 需要尽快处理

信息（Info）：
- 流量峰值接近阈值
- 性能指标异常
- 需要关注

操作步骤

查看告警：
1. 点击左侧菜单"异常峰值告警"
2. 查看告警统计卡片，了解告警概况
3. 查看告警列表，了解详细告警信息
4. 使用筛选条件快速定位告警

处理告警：
1. 在告警列表中找到待处理的告警
2. 点击"处理"按钮
3. 选择处理人
4. 填写处理备注
5. 点击"提交"按钮

标记误报：
1. 在告警列表中找到误报的告警
2. 点击"标记误报"按钮
3. 确认标记操作

归档告警：
1. 在告警列表中找到已处理的告警
2. 点击"归档"按钮
3. 确认归档操作

========================================

4. 节点设备管理

功能概述
统一管理所有被采集的服务器、交换机等网络节点资产。支持节点分组、标签化管理及探针状态监测，为流量实时监控提供准确的采集目标与基础元数据。

主要功能

4.1 节点列表
- 节点ID：节点的唯一标识
- 节点名称：节点的名称
- 类型：服务器/交换机/防火墙
- IP地址：节点的IP地址
- 分组：节点所属的分组
- 标签：节点的标签（支持多标签）
- 状态：在线/离线
- 探针状态：正常/警告/异常
- 位置：节点的物理位置

4.2 节点搜索与筛选
- 关键词搜索：搜索节点名称或IP地址
- 分组筛选：按分组筛选节点
- 状态筛选：按在线/离线状态筛选

4.3 节点操作
- 新增节点：添加新的监控节点
- 编辑节点：修改节点信息
- 查看节点：查看节点详细信息
- 删除节点：删除不需要的节点

4.4 分组管理
- 查看分组列表
- 创建新分组
- 编辑分组信息
- 删除分组

4.5 标签管理
- 为节点添加标签
- 批量修改标签
- 按标签筛选节点

4.6 探针状态监测
- 实时显示各节点的探针状态
- 颜色编码：绿色（正常）/黄色（警告）/红色（异常）
- 探针心跳监测
- 离线自动告警

操作步骤

查看节点列表：
1. 点击左侧菜单"节点设备管理"
2. 查看所有节点的列表信息
3. 使用搜索和筛选功能快速定位节点

新增节点：
1. 点击"新增节点"按钮
2. 填写节点信息：
   - 节点名称（必填）
   - 节点类型（必填）
   - IP地址（必填）
   - 分组
   - 标签（逗号分隔）
   - 带宽
   - 位置
3. 点击"新增"按钮

编辑节点：
1. 在节点列表中找到需要编辑的节点
2. 点击"编辑"按钮
3. 修改节点信息
4. 点击"保存"按钮

查看节点详情：
1. 在节点列表中找到需要查看的节点
2. 点击"查看"按钮
3. 查看节点的完整信息

删除节点：
1. 在节点列表中找到需要删除的节点
2. 点击"删除"按钮
3. 确认删除操作

========================================

5. 告警日志审计

功能概述
集中存储并管理所有异常峰值告警的历史记录，提供全生命周期的事件追踪。支持告警确认、处理指派、误报标记及闭环归档，形成标准化的运维工单流。

主要功能

5.1 告警历史记录
- 查看所有历史告警记录
- 包含告警的完整信息
- 支持按时间、责任人、告警类型检索

5.2 告警处理与确认
- 快速确认告警
- 指派责任人
- 添加处理备注
- 记录处理过程

5.3 误报标记
- 标记误报的告警
- 记录误报原因
- 优化告警策略

5.4 闭环归档
- 将处理完成的告警归档
- 形成标准化运维工单
- 支持历史查询

5.5 统计报表
- 多维检索功能
- 按时间、责任人、告警类型查询
- 生成审计报表
- 可视化数据分析

5.6 审计追踪
- 记录所有告警处理动作
- 操作日志可追溯
- 支持导出审计报告

操作步骤

查看告警历史：
1. 点击左侧菜单"告警日志审计"
2. 查看所有历史告警记录
3. 使用筛选条件快速定位告警

搜索告警：
1. 在搜索框输入关键词
2. 选择状态和级别筛选条件
3. 系统自动过滤显示结果

导出审计报表：
1. 设置筛选条件
2. 点击"导出审计报表"按钮
3. 系统生成并下载报表

========================================

6. 系统与策略设置

功能概述
作为全局控制中枢，负责配置系统运行参数、用户权限及通知渠道。支持自定义采集频率、告警阈值模板及告警收敛降噪规则。

主要功能

6.1 采集配置
- 采集频率：设置数据采集的时间间隔（秒）
- 默认值：60秒
- 可根据实际需求调整

6.2 告警阈值设置

静态阈值：
- 上行带宽阈值（%）：默认80%
- 下行带宽阈值（%）：默认90%
- 95计费峰值阈值（%）：默认95%

动态阈值（AI基线）：
- 启用/禁用AI动态基线
- 基线周期：7天/14天/30天
- 倍数系数：默认1.5倍
- AI模型：LSTM

6.3 告警收敛策略
- 启用/禁用告警收敛
- 收敛窗口（秒）：默认300秒
- 最大告警数：默认5个
- 启用去重：避免重复告警

6.4 通知渠道配置
- 邮件通知：配置SMTP服务器
- 钉钉通知：配置钉钉机器人
- 企业微信通知：配置企业微信机器人
- 支持启用/禁用各渠道
- 支持测试通知

6.5 用户管理
- 查看用户列表
- 用户角色：管理员/运维人员/查看者
- 用户信息：姓名、邮箱、电话
- 权限管理

操作步骤

修改采集频率：
1. 点击左侧菜单"系统与策略设置"
2. 在"采集配置"区域修改采集频率
3. 点击"保存配置"按钮

修改告警阈值：
1. 在"告警阈值设置"区域修改阈值
2. 选择使用静态阈值或动态阈值
3. 配置相应的参数
4. 点击"保存配置"按钮

配置通知渠道：
1. 在"通知渠道配置"区域查看现有渠道
2. 启用/禁用相应的通知渠道
3. 点击"测试通知"按钮验证配置
4. 点击"保存配置"按钮

查看用户列表：
1. 在"用户管理"区域查看用户列表
2. 了解用户的角色和权限

保存配置：
1. 完成所有配置修改后
2. 点击页面底部的"保存配置"按钮
3. 系统提示"配置保存成功"

========================================

退出登录

操作步骤

1. 点击左侧菜单底部的"退出登录"按钮
2. 系统弹出确认对话框
3. 点击"确定"按钮退出系统
4. 或点击"取消"按钮返回系统

========================================

常见问题

Q1: 如何查看实时流量数据？
A: 点击左侧菜单"流量实时监控"，即可查看网络拓扑图和实时流量趋势图。

Q2: 如何导出历史数据报表？
A: 进入"历史数据分析"页面，选择时间范围，点击"导出报表"按钮。

Q3: 如何处理告警？
A: 进入"异常峰值告警"页面，找到待处理的告警，点击"处理"按钮，选择处理人并提交。

Q4: 如何添加新的监控节点？
A: 进入"节点设备管理"页面，点击"新增节点"按钮，填写节点信息并保存。

Q5: 如何修改告警阈值？
A: 进入"系统与策略设置"页面，在"告警阈值设置"区域修改阈值参数，保存配置。

Q6: 如何配置通知渠道？
A: 进入"系统与策略设置"页面，在"通知渠道配置"区域启用/禁用通知渠道，并测试通知。

========================================

技术支持

如有问题，请联系系统管理员。

========================================

文档版本：v1.0
更新日期：2026年9月5日
